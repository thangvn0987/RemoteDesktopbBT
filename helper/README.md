# Ephemeral Helper (Target)

Placeholder for native helper (Rust/C++). Builds outside Docker.

- Captures screen via Desktop Duplication API (Windows)
- WebRTC (libwebrtc)
- SendInput for mouse/keyboard injection
